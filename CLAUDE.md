# Hello Jazz Academy サイトリニューアル

## プロジェクト概要
WordPress + AWSで運用中のジャズピアノ教室サイトを、Next.js（完全静的生成）+ Cloudflare Pages + MicroCMSの構成へ移行する。
既存サイト: https://www.hellojazzacademy.com

## 現行サイトの状況（移行元）
- WordPress 6.9.7 / TCDテーマ(oops_tcd048)
- ページ構成: ホーム、教室について(`/?page_id=83`)、アクセス(`/access/`)、お知らせ(`/news/`)、オンラインレッスン(`/online/`)、お問い合わせ(`/contact/`)、プライバシーポリシー(`/privacypolicy/`)
- 講師2名: 河地里咲（対面・オンライン共に新規募集停止中。現在休業中のため、プロフィールはトップページのみに掲載する方針）、コルテスポール（オンライン新規募集中、英日対応可）
- 現在の運用: ポール先生のオンラインレッスンがメイン。今池教室（千種区）では月1回対面レッスンを実施 — 2027年3月までの期間限定で、それ以降は東京でのレッスンを予定しているが未定
- 拠点: 岩倉教室・天白教室はともに物理教室としてクローズ済み。ただしホームページ上の「天白教室」表示自体は物理教室ゼロに見えないよう当面継続する方針（SEO対策は不要、住所等の掲載のみ）
- 決済: Square請求書を利用。ウェブサイト上に決済機能は実装しない
- 予約カレンダー: 現状使用しない（将来的な導入は未定）
- 生徒専用ページ（ログイン/マイページ）: 現行サイトにUltimate Memberプラグインの仕組みが存在するが、将来実装するかは未定。新サイトでは現時点では実装しない
- 問い合わせフォームのみ（Contact Form 7系のプラグインと推測、要確認）
- 体験レッスン: 45分 ¥3,000（旧: 30分 ¥1,500 → 新サイトで改定）
- オンラインレッスン料金改定: 現行(大人¥10,000/学生¥9,000) → 新料金(大人¥12,000/学生¥10,000)。新サイトは新料金で掲載する
- 既知の不具合: 全ページのOGP画像がプレースホルダー(no-image.gif)のまま → 新サイトで修正すること
- ローカルSEO(旧方針・**廃止**): 旧サイトはメタディスクリプションに対応エリアを明記していた（愛知県岩倉市、一宮、江南、小牧、北名古屋、名古屋市天白区・昭和区・瑞穂区・緑区・南区・千種区、日進市、岐阜県岐阜市・多治見市）。**岩倉教室・天白教室ともに物理教室をクローズしたため、新サイトではこのローカル(エリアベース)SEO方針を踏襲しない**。特に岩倉市関連のSEOは意図的に排除する。天白教室の住所表示は残すがSEO対策の対象外とする

## 目標アーキテクチャ
- フレームワーク: Next.js。`output: 'export'` による完全静的生成のみを使う。SSR/APIルート/ミドルウェアは使わない方針。
- ホスティング: Cloudflare Pages（静的サイトとしてデプロイ。OpenNext/vinextアダプタは使わない）
- CMS: MicroCMS（「お知らせ」「講師」の2コレクションを管理）
- フォーム送信: Formspree または Resend 経由（静的サイトのためサーバーレス/外部サービス必須）
- スタイリング: Tailwind CSS

## デプロイ方法
- Cloudflare PagesはGitHub連携（Cloudflare純正のGitHub App）でリポジトリ(`leap-goals/hello-jazz-academy`)に接続済み。`main`へのマージ/pushが本番ビルド・デプロイのトリガーになる。ローカルでの`wrangler deploy`は使わない（`wrangler.toml`もリポジトリに置いていない）
- PRを作成するとCloudflare Pages側でプレビュービルドが走り、GitHub上に「Cloudflare Pages」チェックとしてビルド結果・ダッシュボードへのリンクが表示される
- リリース手順: 作業ブランチ(`redesign/minimal-editorial`)でPRを作成 → `gh pr merge <PR番号> --merge`でマージコミット方式でマージ（squash/rebaseは使わない。ブランチは削除せず次のPRでも使い回す運用）→ マージ後のmain HEADコミットに対しても同じ「Cloudflare Pages」チェックが走るので、`gh api repos/leap-goals/hello-jazz-academy/commits/<sha>/check-runs`等で本番デプロイの成功(`completed`/`success`)を確認する
- ローカルのNode.jsはv20系のためwrangler CLI(要Node 22+)はそのままでは動かない。ビルドはCloudflare側の環境で完結するため通常は影響しない

## 設計判断とその理由（変更する場合は要議論）
- **完全静的生成を選ぶ理由**: 本サイトにログイン・予約機能等の動的要素はない。Cloudflareの現行の推奨はSSR/APIルートを伴うNext.jsにはWorkers上のvinext(beta)かOpenNextアダプタだが、静的生成に限定すればアダプタ不要でシンプルに保てる。将来動的機能が必須になった場合はVercelへの移行を検討する。
- **MicroCMSを選ぶ理由**: 記事更新者は講師・運営（非エンジニア）。SaaS完結で管理画面が分かりやすいため、Gitコミットベースの編集が必要なDecap CMSより運用が安定する。
- **講師情報をCMSコレクション化する理由**: 現行サイトは講師プロフィールがテーマ側に直書きされている。過去に講師の入れ替わりがあったため、コード変更なしで更新できる形にする。

## 移行時の必須タスク（SEO関連）
- WPのURL構造（例: `/news/スラッグ/`）を新サイトでも踏襲するか、旧→新の301リダイレクトマップを作成する
- 全ページのメタタイトル・ディスクリプションを再構築する。**エリアベースのローカルSEO（岩倉市等）は踏襲しない**。今後のSEOはオンラインレッスンを主軸にする（対応エリアの訴求ではなく、「オンラインジャズピアノレッスン」等のキーワードを重視）
- 全ページにOGP画像を正しく設定する
- 構造化データ(JSON-LD)は物理教室起点のLocalBusinessではなく、オンラインレッスンを主とした内容で追加する（Courseスキーマ中心。天白教室の住所表示分のみ最小限のLocalBusiness情報を残すが、SEO最適化の主眼は置かない）

## 未着手タスク（優先順）
1. WPのREST API (`/wp-json/wp/v2/posts`, `/wp-json/wp/v2/pages`) から全コンテンツをプログラム的に抽出するスクリプトを書く
2. 新旧サイトマップの差分から301リダイレクトマップを生成する
3. Next.jsプロジェクトの雛形を作成する（Tailwind CSS導入、デザインシステム定義）
4. コンポーネント設計（ヘッダー、レッスン一覧、講師紹介、アクセス、お知らせ一覧/詳細、問い合わせフォーム）
5. MicroCMSのコンテンツモデル設計（お知らせ・講師）
6. 切り替え前にPlaywrightで新旧ページの内容差分を自動チェックする

## コンテンツ面のアイデア（余力があれば）
- 演奏動画・音源の埋め込みでジャズセッションの雰囲気を伝える
- Instagram連携で最新投稿を自動表示する
